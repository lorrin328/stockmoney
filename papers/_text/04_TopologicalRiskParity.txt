--- Page 1 ---
Topological Risk Parity∗
Revant Nayar El Mehdi Ainasse Dnyanesh Kulkarni
FMI Technologies LLC FMI Technologies LLC FMI Technologies LLC
April 21, 2026
Abstract
We develop Topological Risk Parity (TRP), a tree-based portfolio construction approach
intended for long/short, market neutral, factor-aware portfolios. The method is motivated by
the dominance of passive/factor flows that naturally create a tree-like structure in markets. We
introduce two implementation variants: (i) a rooted minimum-spanning-tree allocator, and (ii) a
market/sector-anchored variant referred to here as Semi-Supervised TRP, which imposes SPY as
the root node and the 11 sector ETFs as the second layer. In both cases, the key object is a
sparse rooted topology extracted from a correlation-distance graph, together with a propagation
law that maps signed signals into portfolio weights.
Relative to classical Hierarchical Risk Parity (HRP), TRP is non-binary and designed for
signed cross-sectional signals and hedged long-short portfolios: it preserves signal direction while
using return-dependence geometry to shape exposures. It accounts for the fact that there is
imperfect correlation between parent and child nodes, and thus does not propagate weights
entirely to the children. We can also impose economically motivated hierarchy that involves
industries, sub-industries or factors, etc. This makes it much more robust to macroeconomic
shocks and crises, where within-cluster correlations might spike. These features make TRP well
suitedformarket-neutral,equitystat-arborL/Strend-typestrategies,whereenforcingneutrality
or limiting exposures at the market, sector or factor level is extremely important.
1 Introduction
Portfolioconstructionsitsattheintersectionofcross-sectionalalphamodelinganddependence-aware
risk control. Mean–variance methods in the tradition of [1] provide a canonical baseline but can
be unstable in high dimensions and sensitive to covariance estimation. Network and hierarchy-
based methods, including minimum-spanning-tree representations of correlation structure [2] and
Hierarchical Risk Parity (HRP) [5], aim to regularize this problem by replacing dense covariance
optimization with sparse or recursive structure. However, HRP based methods do not account for
long-short exposures and propagate weights entirely to the child nodes.
This paper formalizes a class of algorithms that start from the same broad intuition—use the
geometry of return dependence to regularize raw signals—but implement that idea in a markedly
different way. The approach is based on the financial intuition that the market is organised into
hierarchies of clusters and sub-clusters with distinct dynamics, with correlated dynamics introduced
by the rise of ETFs and passive investing. We start with a rooted MST allocator, and we also
introduce a dummy market root that forces sector ETFs to form the second layer of the hierarchy.
We derive the final portfolio by multiplying the original signal of each asset by a topological factor
∗This document is for informational purposes only and does not constitute investment advice.
1
6202
rpA
81
]MP.nif-q[
1v37761.4062:viXra

--- Page 2 ---
determined by its position in the rooted topology, followed by an L1 normalization to the desired
leverage.
The resulting construction is close in spirit to HRP, but differs in its core object and recursion:
TRP uses a rooted sparse topology and propagates signed signal mass through that topology. The
localnon-conservationatbranchingnodesisadeliberatefeature: itletsthehierarchyshapeexposure
while still retaining idiosyncratic positions, rather than forcing a purely conservative split at every
node.
Our goal is fourfold:
(i) give a complete mathematical description of the framework;
(ii) characterize its exact departure from HRP;
(iii) establish basic theoretical results and bounds that explain the behavior of the method;
(iv) discuss practical implications, benefits, and limitations relative to HRP.
2 Related background
The present work is closest to three lines of research.
First, correlation-network methods represent financial universes as weighted graphs, often using
the Mantegna distance
r1−ρ
d = ij ,
ij 2
whose MST extracts a sparse backbone of the dependence structure [2]. This produces a graph with
n−1 edges on n vertices, emphasizing the most essential pairwise relations.
Second, hierarchical allocation methods such as HRP construct a dendrogram from hierarchical
clustering and allocate capital recursively between clusters, typically using cluster variance as the
splitting criterion [5]. HRP conserves capital at each split and is parity-inspired in the sense that
recursive cluster allocations seek balanced risk budgets.
Third, signal overlay methods combine a signal vector with exogenous regularization or sparse
structure. TRP belongs to this family: the raw signal remains central, but it is filtered and rescaled
through a rooted topological object inferred from returns.
3 MSTs and Passive/ Factor Flows
Thissectiongivesasimple(stylized)mechanismlinkingallocatablefactorflowstotree-likecorrelation
structure. The conclusion is not that markets are exactly tree-structured, but that when return co-
movement is dominated by a small number of nested common components, the correlation-distance
geometry becomes strongly ordered, and an MST extracts a natural sparse backbone.
Correlation distance and MST backbone. Let Σ ∈ RN×N denote the covariance matrix of
returns, with correlations
Σ
ρ := ij .
ij pΣ Σ
ii jj
Define the Mantegna correlation distance
r1−ρ
D := ij . (1)
ij 2
2

--- Page 3 ---
A minimum spanning tree (MST) on the complete weighted graph with weights D selects a set of
ij
N −1 edges minimizing total distance. In regimes where the distances fall into well-separated tiers,
the MST tends to connect within the closest tier first (basket), then the next (sector), and finally
across sectors.
A three-level nested flow model. Assume each asset i belongs to a sector s(i) and a basket
b(i) (e.g., an ETF sleeve, thematic basket, or a tight industry group). Model returns as
r = θ z +θ z +θ z +ε , (2)
i M M S s(i) B b(i) i
where z , {z }, {z } are mutually independent shocks with unit variance, and idiosyncratic noise
M s b
satisfies E[ε ] = 0, Var(ε ) = σ2, and Cov(ε ,ε ) = 0 for i ̸= j.
i i ε i j
Implied covariance tiers. For i ̸= j,

 θ M 2 +θ S 2 +θ B 2, b(i) = b(j),
Cov(r ,r ) = θ2 +θ2, s(i) = s(j), b(i) ̸= b(j), (3)
i j M S
θ2
, s(i) ̸= s(j).
M
Moreover,
Var(r ) = θ2 +θ2 +θ2 +σ2. (4)
i M S B ε
Therefore correlations satisfy the strict ordering
ρ > ρ > ρ , (5)
samebasket samesector differentsector
and the associated distances satisfy
D < D < D . (6)
samebasket samesector differentsector
Ultrametric intuition (idealized). In the noiseless limit σ2 → 0 with distinct tiers, pairwise
ε
distances take only three values, determined by the deepest shared level (basket/sector/market).
This is the qualitative hallmark of an ultrametric or tree-like metric, in which distances are largely
determined by the lowest common ancestor.
Passive/ factor dominance knob. Tomaketheorderingstrengthexplicit,onemayparameterize
common-flow strength with λ ∈ [0,1] via
√ √
r = λ(z +z +z )+ 1−λε , (7)
i M s(i) b(i) i
with Var(ε ) = 1. As λ → 0, correlations collapse and distances become harder to separate, making
i
the MST more sample-noise-driven; as λ → 1, the tiers in (5)–(6) separate cleanly, making the MST
backbone more stable.
Star versus deep hierarchy. If θ2 ≫ θ2,θ2, then all names are dominated by the market
M S B
component, correlations become nearly uniform, and the MST tends to degenerate toward a star-like
structure (a single market hub). A deep hierarchy is most visible when multiple common-flow levels
coexist, e.g.
θ2 ,θ2,θ2 ≫ σ2, with θ ,θ ̸≪ θ .
M S B ε S B M
3

--- Page 4 ---
Implication for TRP. TRP can be viewed as a signal allocator that conditions on such a
sparse backbone: it uses an MST-derived (or economically anchored) rooted tree to attenuate and
redistribute signed signals in a way that is consistent with dominant co-movement structure, while
remaining simpler and more robust than dense covariance optimization.
4 Setup and notation
Let N denote the total number of assets. For each asset i ∈ {1,...,N}, let
s ∈ R
i
be the current signed signal, and let
R = (r ,...,r ) ∈ RT
i i1 iT
be a history of returns or log-returns. Stack these into
s = (s ,...,s )⊤ ∈ RN, R ∈ RN×T.
1 N
4.1 Activity filter
Fix a lookback window k ≤ T, a minimum recent-magnitude threshold ε > 0, and a signal threshold
τ > 0. Define
1 T
m := X |r |,
i it
k
t=T−k+1
and the active set
A := {i ∈ {1,...,N} : m > ε, |s | > τ}. (8)
i i
In our baseline specification we take τ = 10−3 and restrict attention to the active assets A.
Let n := |A|, and relabel the active assets as 1,...,n when convenient. Denote the restricted
A A
signal and return matrix by
s(A) ∈ RnA, R(A) ∈ RnA×T.
4.2 Correlation distance and the MST
From the active return history, form the sample correlation matrix
C = corr(R(A)) ∈ [−1,1]nA×nA.
The implementation clips C entrywise to [−1,1], replaces NaNs by zero, and symmetrizes. The
associated distance matrix is
s
1−C
D = ij . (9)
ij 2
This is the standard correlation distance used in network representations of financial markets.
Construct the complete weighted graph
!
V
G = (V,E,D), V = {1,...,n }, |E| = ,
A 2
with edge weight D . Let MST(G) denote a minimum spanning tree of G.
ij
4

--- Page 5 ---
Proposition 1 (Basic distance bounds). For all i,j, the distance (9) satisfies
0 ≤ D ≤ 1.
ij
Moreover, D = 0 if C = 1, and D = 1 if C = −1.
ij ij ij ij
Proof. Since C ∈ [−1,1], we have (1−C )/2 ∈ [0,1], hence its square root also lies in [0,1]. The
ij ij
endpoint statements follow immediately.
Remark 1. The MST compresses a dense n (n −1)/2-edge graph into a sparse backbone with
A A
exactly n −1 edges. This sparsification is one reason TRP is attractive as a regularization device:
A
the final allocator depends only on a tree or spanning tree rather than on all pairwise interactions.
5 Topological Risk Parity (TRP)
We now formalize two variants.
5.1 Variant I: rooted MST allocation
We first describe the generic rooted MST variant.
Step 1: active universe and MST. Given the active set A, compute the MST on the distance
graph of active assets.
Step 2: choose a root. Let T = (V ,E ) be the undirected MST. Choose a root r ∈ V by one
T T T
of the following rules:
• hub mode: a node of maximal degree in the MST;
• max-magnitude mode: a node with maximal |s |;
i
• fixed-index mode: a designated index, often the first active node.
Once r is chosen, orient the tree away from r. Because an undirected tree has a unique simple path
between any two nodes, the parent/child relation induced by a fixed root is unique; any traversal
can be used to recover it.
For any non-root node v, let pa(v) denote its (unique) parent node, and let Ch(u) denote the
set of children of node u. Define the branching number
b(u) := |Ch(u)|.
Step 3: mixed split-replication coefficient. Fix a parameter ρ ∈ [0,1]. For each internal node
u with b(u) ≥ 1, define
ρ
α (ρ) := (1−ρ)+ . (10)
u b(u)
This is the key propagation coefficient.
5

--- Page 6 ---
Step 4: topological factor. Initialize the root with factor
g = 1.
r
For any non-root node v, define recursively
g = α (ρ)g . (11)
v pa(v) pa(v)
Equivalently, if Path(r,v) denotes the ordered set of ancestors of v on the unique path from r to v
(including r and excluding v itself), then
g = Y α (ρ). (12)
v u
u∈Path(r,v)
Step 5: pre-normalization portfolio. The implementation uses the original signal, not a
volatility-rescaled signal:
x = s(A)g .
v v v
Step 6: leverage normalization. Let L > 0 be the target gross leverage. The final weight on
active assets is
x
w = L v , v ∈ A, (13)
v
∥x∥
1
provided ∥x∥ > 0. Inactive assets receive zero weight.
1
Remark (independence from subtree-mass exponent). One may compute subtree quantities
S = X |s |p
u i
i∈subtree(u)
for an exponent p ≥ 1, but in Variant I these quantities are not used in the propagation. As a result,
for fixed active set, root, and return history, the output is independent of p.
5.2 Variant II: market/sector-anchored Semi-Supervised TRP
We now describe the second variant, which we call TRP-SPY/XL. The aim is to impose an
economically meaningful prior:
market root → sector ETFs → other assets.
One can also impose an industry or sub-industry structure using ETFs or GICS labeling.
market root → sector ETFs → industry ETFs → other assets.
One can also introduce a factor-based hierarchy underneath sectors (eg. growth vs value stocks
within a sector), thereby controlling factor exposures as well. In this framework, one can introduce
neutrality at the sector, industry or factor level by imposing that P w = 0 at that layer.
i i
Step 1: dummy market root. Introduce a dummy node m labeled SPY. The dummy node is
assigned signal 0 and zero returns in the fallback branch. It does not receive portfolio weight in the
final output; it exists only to organize the hierarchy.
6

--- Page 7 ---
Step 2: identify sector ETFs. Let
X = {i ∈ A : ticker begins with XL}
i
be the active set of sector ETFs. In practice this captures names such as XLK, XLF, and XLE.
Case A: sector ETFs present. When X ̸= ∅, the implementation:
(a) builds an MST on the real active assets only;
(b) augments the resulting tree by adding the dummy root m and connecting m to every sector
ETF in X;
(c) runs a DFS from m to produce a rooted spanning tree.
A subtle but important point is that step (b) turns a tree on n real assets into a connected
A
graph that generally contains cycles whenever |X| > 1. The subsequent DFS does not merely orient
a tree; it extracts a rooted spanning tree from this connected graph.
Case B: no sector ETFs present. WhenX = ∅, theimplementationfallsbacktoanaugmented-
correlation construction: the dummy root is inserted as another node and the MST is computed on
the augmented universe. This is a pragmatic engineering fallback, but it is less theoretically clean
because the dummy node has zero returns and therefore artificial correlations.
Propagation and normalization. Oncetherootedspanningtreeisformed,thesamepropagation
law (10)–(13) is applied to the real assets. Unlike Variant I, the Semi-Supervised TRP variant then
applies post-processing:
w ← clip(w ;−c,c), w ← 0 if |w | < η, (14)
i i i i
for a cap c > 0 and a minimum absolute weight threshold η > 0.
Remark 2. Thesector-anchoredvariantissometimesdescribedinformallyasanequal-splitallocator.
In the present formulation, it uses the same ρ-based propagation rule (10) as Variant I; the equal-split
conservative limit corresponds to setting ρ = 1.
6 Theoretical properties
We now collect a set of exact identities and basic theoretical results.
6.1 Path formula and immediate consequences
Proposition 2 (Exact path representation). For either TRP variant, once the rooted tree or
spanning tree is fixed, each node v has topological factor
g = Y (cid:18) (1−ρ)+ ρ (cid:19) .
v b(u)
u∈Path(r,v)
Proof. This follows by repeated substitution of the recursion (11) along the unique parent chain
from the root to v.
7

--- Page 8 ---
Proposition 3 (Limit cases). For fixed rooted tree:
(a) if ρ = 0, then α (0) = 1 for every internal node u, hence g = 1 for all v, and the allocator
u v
reduces to normalized raw-signal allocation:
(A)
s
w = L v ;
v (cid:13) (cid:13)
(cid:13)s(A)(cid:13)
1
(b) if ρ = 1, then α (1) = 1/b(u) and the allocator becomes a conservative equal-split rooted-tree
u
method.
Proof. Part (a) is immediate from (10); so is part (b).
Remark 3. The parameter ρ interpolates between no topological attenuation (ρ = 0) and equal-
split attenuation (ρ = 1). This interpolation is central: topology enters continuously rather than
discretely.
6.2 Exact quantification of non-conservation
Proposition 4 (Local mass amplification). Let u be an internal node with branching number b(u).
Then the sum of the immediate child factors satisfies
X g = β (ρ)g , β (ρ) := b(u)(1−ρ)+ρ. (15)
v u u u
v∈Ch(u)
Moreover,
β (ρ) ≥ 1,
u
with equality if and only if either b(u) = 1 or ρ = 1.
Proof. By construction, every child v of u satisfies
(cid:18) ρ (cid:19)
g = α (ρ)g = (1−ρ)+ g .
v u u b(u) u
Summing over the b(u) children gives
X g = b(u) (cid:18) (1−ρ)+ ρ (cid:19) g = (b(u)(1−ρ)+ρ)g .
v b(u) u u
v∈Ch(u)
Since
β (ρ)−1 = (b(u)−1)(1−ρ) ≥ 0,
u
the claim follows.
Corollary 1 (Local expansion relative to conservative splitting). The exact local deviation from
conservative splitting is
β (ρ)−1 = (b(u)−1)(1−ρ).
u
Equivalently, whenever ρ < 1 and b(u) > 1, branching expands total descendant factor mass relative
to the conservative benchmark β (ρ) = 1.
u
8

--- Page 9 ---
Corollary 2 (Level-mass bound). Let
Γ (ρ) := B(1−ρ)+ρ,
B
where B is the maximal branching number. If L denotes the set of nodes at depth ℓ, then
ℓ
X g ≤ Γ (ρ)ℓ.
v B
v∈L
ℓ
Proof. At depth 0, the only node is the root and the claim is true since g = 1. Moving from depth
r
ℓ to ℓ+1, the total factor mass generated by each node is multiplied by at most Γ (ρ) by (15).
B
Induction gives the result.
Interpretation. The local factor g along any single path cannot exceed the ancestor factor g ,
v u
yet the aggregate mass across descendants can expand when branching is present and ρ < 1. This is
the precise sense in which the framework amplifies topology while attenuating along each individual
path.
6.3 Signal preservation and normalization
Proposition 5 (Sign preservation). Suppose post-processing (14) is not applied. If s (A) ̸= 0, then
v
sign(w ) = sign(s(A)).
v v
Proof. Since g > 0 for all v and L/∥x∥ > 0, the sign of w equals the sign of x = s (A) g .
v 1 v v v v
Proposition 6 (Scale symmetry). Suppose post-processing (14) is not applied. For any scalar
c > 0,
w(cs) = w(s),
and for any c < 0,
w(cs) = −w(s).
Proof. Because x(cs) = cx(s) and the final output divides by ∥x(cs)∥ = |c|∥x(s)∥ , positive
1 1
rescalings cancel and negative rescalings flip sign.
6.4 Sector-anchored structure in Semi-Supervised TRP
We now formalize the one strong structural guarantee provided by the sector-anchored variant.
Proposition 7 (Depth-one sector ETFs). Assume the real-asset MST is connected and X ≠ ∅.
Construct the augmented graph by adding the dummy root m and edges (m,x) for every x ∈ X. In
the DFS spanning tree rooted at m produced by the implementation, every sector ETF x ∈ X is a
depth-one child of m.
Proof. In any spanning tree rooted at m that includes all edges (m,x) for x ∈ X as tree edges,
every sector ETF x ∈ X is a depth-one child of m.
Proposition 8 (Connectivity of the sector-anchored construction). Assume the real-asset MST
is connected and X ̸= ∅. Then the augmented graph obtained by adding the root m and the edges
(m,x) for x ∈ X is connected, and the DFS parent map defines a spanning tree over all vertices.
9

--- Page 10 ---
Table 1: Conceptual comparison between TRP and classical HRP.
Feature TRP-MST TRP-SPY/XL Classical HRP
(Semi-Supervised TRP)
Primary structure Rooted MST Rooted spanning tree Binary dendrogram
with market/sector
prior
Support for L/S Yes Yes No
portfolios
Root Heuristic or fixed Dummy market root No exogenous root
(SPY)
Second layer Emergent Sector ETFs (XL*) if Emergent from
present clustering
Propagation object Signed signals Signed signals Cluster variance / risk
budget
Local conservation No, unless ρ = 1 No, unless ρ = 1 Yes
Uses original signal Yes Yes Not central to vanilla
sign HRP
Requires covariance No No No
inversion
Requires cluster No No Yes
variance recursion
Economic priors can Limited Strong Limited
be imposed
Proof. The real-asset MST is connected by definition. Since m is connected to every node in X, and
X is nonempty, m is connected to the real-asset tree. Therefore the augmented graph is connected.
A DFS on a connected graph always generates a spanning tree.
Remark 4 (Order dependence). When |X| > 1, the augmented graph contains cycles. The sector
ETFs remain depth-one children of the root, but the parent assignments of non-sector assets can
depend on DFS traversal order. This is a genuine implementation feature of the current code and
should be acknowledged explicitly.
7 Practical implications and benefits
7.1 Benefits relative to HRP
The framework offers several practical advantages.
1. Explicit control over hierarchy. The sector-anchored Semi-Supervised TRP variant allows
the user to encode a market/sector/stock ontology directly. This is difficult in vanilla HRP, where
the hierarchy is entirely emergent from clustering.
10

--- Page 11 ---
2. Support for L/S portfolios TRP multiplies the original signal by a positive topological
factor, so the sign of the alpha is preserved prior to any clipping or thresholding. This makes
the method natural for long-short overlays. From a practitioner perspective, this matters because
long–short books are typically built from signed signals and then constrained by gross leverage, net
exposure, andpositioncaps. TRPpreservesthelong/shortintentofthesignalbyconstruction, while
using the dependence topology to tilt position sizes away from crowded clusters and toward more
idiosyncratic names—often reducing unintended factor bets without running a fragile covariance
optimizer. In live workflows, the final weights still drop out in one pass as a vector that can be
clipped, thresholded, sector-neutralized, and rescaled to the desk’s risk limits.
3. Economic Interpretability TRP reflects underlying factor hierarchies such as one might see
in Barra or Axioma models. While HRP only allows for binary trees, TRP captures the natural
factor hierarchy that exists in markets, which involves the market branching into sectors, industries
and sub-industries. One can impose or recover through the MST, different factor clusters that might
exist within sectors or industries. One can then impose market, sector or factor neutrality easily.
4. Sparse and interpretable structure. The allocator depends on a tree or spanning tree
rather than on the full covariance matrix. This creates an intuitive map from economic or statistical
structure to final position sizes.
5. Smooth interpolation parameter. The parameter ρ continuously interpolates between
normalized raw-signal allocation (ρ = 0) and equal-split rooted-tree allocation (ρ = 1). This is a
flexible control lever absent from standard HRP.
6. No covariance inversion and no recursive cluster variance estimation. Like HRP,
TRP avoids covariance inversion. Unlike HRP, it does not require cluster variance calculations or
quasi-diagonalization. The pipeline is comparatively simple: correlation matrix, distance matrix,
MST, rooted propagation, normalization.
7. Natural compatibility with post-processing constraints. Capping, pruning of very small
weights, gross leverage targeting, and sector-specific overlays are easy to append after the rooted
propagation step.
8. Resilience to Macro Shocks Correlations that were low in-sample might drastically spike
during macroeconomic shocks. Here the economically motivated tree saves us by ensuring that we
have limited allocation to a sector, industry or factor.
7.2 Modeling choices and caveats
These benefits come with modeling choices that should be understood.
1. Root dependence matters. In the generic rooted-MST variant, the root can be chosen by
degree, maximal signal, or index. Different roots can change the entire allocation.
2. MST instability matters. Small changes in correlations can change the MST combinatorially.
This sensitivity is shared by many graph-based methods.
11

--- Page 12 ---
3. A single tree discards edges. TheMSTkeepsonlyn −1edges,whichcreatesinterpretability
A
and sparsity but necessarily discards information present in the full graph.
4. Order dependence in the sector-anchored construction. When multiple sector ETFs
are connected to the dummy root, the augmented graph contains cycles. The resulting spanning
tree for non-sector nodes can depend on traversal order.
5. The fallback dummy-root MST branch is pragmatic, not canonical. Treating a
zero-return dummy root as a node in the augmented correlation matrix is a pragmatic fallback
rather than a theoretically ideal construction.
7.3 Useful structural facts
The framework is simple enough that several properties are immediate.
1. Sign preservation As long as clipping does not zero out the name, the final weight has the
same sign as the original signal:
sign(w ) = sign(s ).
i i
2. Scale invariance of the signal If all signals are multiplied by the same positive constant,
the final normalized weights are unchanged. If all signals are multiplied by a negative constant, all
positions flip sign.
3. Topology only attenuates along a path Since
ρ
α (ρ) = (1−ρ)+ ≤ 1,
u b(u)
every additional branching level weakens the surviving conviction of downstream names. The deeper
a node sits in the tree, the more structure it must pass through before becoming a final position.
4. The framework collapses to raw-signal allocation when topology is ignored At ρ = 0,
the framework simply returns a normalized signal portfolio. This provides a useful baseline and
allows one to measure whether the topology is actually helping.
8 Conclusion
We began with the intuition that passive and factor flows in markets create a tree-like topological
structure, captured by minimal spanning trees (MSTs). We have formalized Topological Risk
Parity built around rooted sparse dependence structures captured by MSTs and signal-preserving
propagation. The framework captures the essential logic of two variants: a generic rooted MST
allocator and a market/sector-anchored Semi-Supervised TRP variant.
The main theoretical conclusion is simple and important. The propagation rule
ρ
α (ρ) = (1−ρ)+
u b(u)
interpolates between a non-conservative regime (ρ < 1), in which branching can expand aggregate
descendant factor mass, and the conservative equal-split regime (ρ = 1). This continuous control is
the mechanism by which topology shapes exposure.
12

--- Page 13 ---
Overall, TRP offers explicit economic hierarchy, sparse interpretability, preservation of signal
sign, and simple integration of practical constraints. The main sensitivities are root choice, MST
sensitivity to correlations, and (in the sector-anchored construction) order dependence for non-sector
descendants.
A Alternative statement of the generic allocator
For clarity, the generic TRP-MST variant can be summarized in a compact formula. Let T be the
r
rooted MST on the active assets with root r, and define
g (T ,ρ) = Y (cid:18) 1−ρ+ ρ (cid:19) .
i r b(u)
u∈Path(r,i)
Then the final portfolio is simply
s g (T ,ρ)
w = L i i r ·1 , (16)
i P |s |g (T ,ρ) {i∈A}
j∈A j j r
followed, if desired, by clipping and thresholding.
B Recommended terminology
To avoid conceptual confusion, the following naming convention is recommended:
• TRP-MST: the generic rooted MST allocator;
• TRP-SPY/XLorSemi-Supervised TRP:thesector-anchoredvariantwithdummymarket
root;
• topological allocation: the generic family name;
• topological risk parity: use only for conservative, risk-based variants, not for the generic
TRP-MST construction when ρ < 1.
C Additional theoretical results
This appendix collects supplementary results that are not needed to implement TRP but may be
useful for analysis.
C.1 Bounds on topological factors
Proposition 9 (Bounds on topological factors). Assume ρ ∈ [0,1], and let
B := max b(u)
u:b(u)≥1
be the maximal branching number in the rooted tree. Then for every node v of depth d := depth(v),
v
(cid:18) ρ(cid:19)dv
1−ρ+ ≤ g ≤ 1. (17)
v
B
In particular,
(1−ρ)dv ≤ g ≤ 1.
v
13

--- Page 14 ---
Proof. For every internal node u,
ρ ρ
1−ρ+ ≤ 1−ρ+ ≤ 1,
B b(u)
since b(u) ≤ B and ρ ∈ [0,1]. Multiplying along a path of length d yields (17).
v
C.2 Independence from the subtree-mass exponent in Variant I
Proposition 10 (Independence from p in Variant I). Fix the active set A, returns R(A), root r,
and parameter ρ. In Variant I, the output is independent of the exponent p used to compute subtree
masses.
Proof. One may compute
S = X |s |p,
u i
i∈subtree(u)
but in Variant I the propagation coefficients are determined solely by the branching numbers via
(10). The subtree masses never enter the final weight computation. Therefore, once the tree itself is
fixed, the output is independent of p.
C.3 Conditional Lipschitz stability
The topological map is piecewise smooth: it is smooth conditional on a fixed active set, fixed
correlation matrix ordering, and fixed rooted tree. Discontinuities arise only when the filter changes,
when correlations cross enough to alter the MST, or when the root rule changes.
Theorem 1 (Conditional L1 stability). Fix a rooted tree and its topological factors g . Let
v
D := diag(g ,...,g ), and define
g 1 nA
D s
F(s) := L g ,
∥D s∥
g 1
whenever ∥D s∥ > 0. If ∥D s∥ ≥ γ and ∥D s′∥ ≥ γ for some γ > 0, then
g 1 g 1 g 1
2L 2L
(cid:13) (cid:13)F(s)−F(s′)(cid:13) (cid:13) 1 ≤ γ (cid:13) (cid:13)D g (s−s′)(cid:13) (cid:13) 1 ≤ γ (cid:13) (cid:13)s−s′(cid:13) (cid:13) 1 . (18)
Proof. Set x = D s and y = D s′. Then
g g
x y x−y 1 1 !
− = +y − .
∥x∥ ∥y∥ ∥x∥ ∥x∥ ∥y∥
1 1 1 1 1
Taking L1 norms and using ∥x∥ ,∥y∥ ≥ γ,
1 1
(cid:13) (cid:13) x y (cid:13) (cid:13) ∥x−y∥ (cid:12) (cid:12)∥y∥ −∥x∥ (cid:12) (cid:12) 2∥x−y∥
(cid:13) − (cid:13) ≤ 1 + 1 1 ≤ 1.
(cid:13)∥x∥ ∥y∥ (cid:13) γ γ γ
(cid:13) 1 1(cid:13) 1
Multiplying by L proves the first inequality. The second follows because every g ≤ 1 by (17), hence
v
∥D z∥ ≤ ∥z∥ for all z.
g 1 1
Remark 5. The theorem is intentionally conditional. It does not claim global continuity across
changes in the MST, root, or active set. Those combinatorial changes are real and should be handled
empirically.
14

--- Page 15 ---
References
[1] H. Markowitz. Portfolio Selection. The Journal of Finance, 7(1):77–91, 1952.
[2] R. N. Mantegna. Hierarchical Structure in Financial Markets. The European Physical Journal
B, 11:193–197, 1999.
[3] J. B. Kruskal. On the Shortest Spanning Subtree of a Graph and the Traveling Salesman
Problem. Proceedings of the American Mathematical Society, 7(1):48–50, 1956.
[4] R. C. Prim. Shortest Connection Networks and Some Generalizations. Bell System Technical
Journal, 36(6):1389–1401, 1957.
[5] M. Lopez de Prado. Building Diversified Portfolios that Outperform Out of Sample. The
Journal of Portfolio Management, 42(4):59–69, 2016.
Disclaimer
This document is provided for informational and educational purposes only and does not constitute
investment advice, a recommendation, or an offer to buy or sell any securities, financial instruments,
or investment strategies. The authors make no representations or warranties as to the accuracy,
completeness, or suitability of the information contained herein. The views expressed are subject to
change without notice and may not reflect the opinions of any affiliated institutions or entities.
15